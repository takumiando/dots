#!/bin/sh

cd "$(dirname "$0")" || exit

for f in .??*
do
    case "$f" in
        .git*)
            ;;
        *)
            ln -snfv "$PWD"/"$f" ~/"$f"
            ;;
    esac
done

find config -type f | while read -r f
do
    ln -snfv "$PWD"/"$f" ~/."$f"
done
